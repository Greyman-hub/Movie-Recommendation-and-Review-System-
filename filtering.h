#ifndef FILTERING_H
#define FILTERING_H

// Declare the filtering function here
char* filter_movies_for_gui(const char *genre, const char *language, const char *decade_str, float min_rating);

#endif
